# Bundled RIFE model packages: files.json contains size/CRC32 checks over these
# exact bytes, so line-ending conversion must never touch them.
Pkgs/rife-cuda/RIFE425/** -text
Pkgs/rife-cuda/RIFE426/** -text
